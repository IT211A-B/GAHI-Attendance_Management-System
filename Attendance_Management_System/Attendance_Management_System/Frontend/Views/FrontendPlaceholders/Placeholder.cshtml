@model Attendance_Management_System.Backend.ViewModels.Frontend.PlaceholderPageViewModel
@{
    ViewData["Title"] = Model.Title;
}

<section class="panel">
    <h3>@Model.Title</h3>
    <p class="muted">@Model.Description</p>

    <ul class="list">
        <li><strong>Route parity completed:</strong> @Model.RoutePath</li>
        <li><strong>Frontend stack:</strong> C# MVC + Razor</li>
        <li><strong>Status:</strong> Waiting for matching backend API endpoints</li>
    </ul>
</section>
